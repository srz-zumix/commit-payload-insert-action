name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./test/test-action
    - uses: ./
      with: 
        commit: 6bb00a4
    - uses: ./test/test-action

        
